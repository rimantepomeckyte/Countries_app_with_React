import React from 'react';

const Header = () => {
    return (
        <header className="row d-flex justify-content-center">
            <h1 className="pt-4">Find country!</h1>
        </header>
    );
};

export default Header;